

    		<td class="wrapper {{>offsetLogic}} {{#if end}}last{{/if}}">
	<table class="{{col-classes}} {{>columnLogic}} columns">
		<tr>
    		<td class="{{classes}}{{#is pad 'left'}} left-text-pad{{/is}}{{#is pad 'right'}} right-text-pad{{/is}}{{#is pad 'both'}} text-pad{{/is}}{{#is pad 'none'}}{{/is}}{{#unless pad}} text-pad{{/unless}} {{>centerLogic}}">